# App icons

Icon sources live under this directory per channel (e.g. dev / beta / prod). See `scripts/copy-icons.ts` for how they are applied during build.

For macOS `.icns` generation, tools such as [Image2Icon](https://img2icnsapp.com/) can help match platform padding expectations.
